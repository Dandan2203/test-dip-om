# Аудит функціональності FinAgent

Статуси визначені за поточним кодом, а не за попередніми описами.

| Заявлена функція | Фактичний статус | Доказ у коді | Ризик на захисті | Рекомендоване формулювання | Потрібні зміни |
|---|---|---|---|---|---|
| Реєстрація й автентифікація | Реалізовано | `backend/internal/delivery/http/router.go`, `user_handler.go`, `pkg/auth` | Низький | Реєстрація та JWT-автентифікація | Немає |
| Перегляд і редагування профілю | Реалізовано | `user_handler.go`, `user_usecase.go`, `frontend/src/pages/ProfilePage.tsx` | Низький | Перегляд і редагування профілю | Немає |
| CRUD транзакцій | Реалізовано | `transaction_handler.go`, `transaction_usecase.go`, `TransactionsPage.tsx` | Низький | Створення, перегляд, редагування й видалення транзакцій | Немає |
| Історія з пагінацією та фільтрами | Реалізовано | API підтримує `limit`, `offset`, дати, тип і категорію; UI має фільтри та перемикання сторінок | Низький | Фільтрований список транзакцій із пагінацією | Немає |
| Системні й власні категорії | Реалізовано | `category_usecase.go`, `category_repository.go`, `CategoriesPage.tsx` | Низький | Системні та користувацькі категорії | Немає |
| AI-категоризація витрат | Частково реалізовано | `/categorize` у `financial-ai-agent/main.py` і `backend/internal/ai/client.go::Categorize`; виклик не підключений до користувацького сценарію | Високий | Експериментальний сервіс пропозиції категорії за описом | Підключити до створення транзакції або чесно залишити додатковим |
| Аналітика та графіки | Реалізовано | `stats_handler.go`, `stats_repository.go`, `frontend/src/components/widgets` | Низький | Базова аналітика доходів і витрат | Немає |
| CSV-експорт | Реалізовано | `stats_handler.go::Export`, `stats_repository.go::ExportRows`, `TransactionsPage.tsx::handleExport` | Низький | Експорт відфільтрованих транзакцій у CSV | Виправлено фільтр категорії та виключення видалених записів |
| Фінансові цілі | Реалізовано | `goal_handler.go`, `goal_usecase.go`, `GoalsPage.tsx` | Низький | Цілі та відстеження прогресу | Немає |
| AI-чат про фінанси | Реалізовано | `chat_handler.go`, `agents/chat.py`, `ChatPanel.tsx` | Низький | AI-асистент відповідає на запитання про доступні фінансові дані | Немає |
| Сценарії «що, якби» | Реалізовано | `agents/tools.py::simulate_purchase` | Низький | Просте моделювання впливу покупки на місячний баланс | Не називати прогнозуванням |
| Персоналізовані рекомендації | Частково реалізовано | Загальні відповіді чату спираються на контекст; окремого алгоритму рекомендацій немає | Середній | Загальні пояснення та поради в AI-чаті на основі доступних даних | Не заявляти окремий модуль рекомендацій |
| Виявлення нетипових витрат | Реалізовано | `agents/auditor.py`, `/api/anomalies`, `AnomaliesPage.tsx`, `AnomaliesWidget.tsx` | Низький | Виявлення нетипових витрат і показ попереджень | Не заявляти фонові сповіщення |
| Дашборд | Реалізовано | `/api/dashboard`, `DashboardPage.tsx`, віджети | Низький | Дашборд зі зведенням і останніми операціями | Немає |
| Підтвердження AI-дій | Реалізовано | `chat_handler.go::resolveAction`, `ChatPanel.tsx::confirmPending` | Низький | Кожна AI-дія зі зміною даних потребує підтвердження | Узгоджено для всіх дій |
| Monobank | Додаткова | `mono_usecase.go`, `mono_handler.go`, профіль UI | Середній | Додатковий модуль імпорту Monobank | Не робити центральною функцією |
| Налаштовуваний дашборд | Додаткова | `dashboard_usecase.go`, `DashboardGrid.tsx` | Низький | Додаткове налаштування віджетів | Не акцентувати |
| Eval і CI | Додаткова | `eval/`, `.github/workflows/ci.yml`, `.gitlab-ci.yml` | Низький | Допоміжні засоби перевірки розробки | Не подавати як функцію користувача |

## Виправлено під час аудиту

CSV-експорт застосовує фільтр категорії та не включає м’яко видалені транзакції. Інтерфейс транзакцій має просту пагінацію. Усі AI-дії, що змінюють дані, потребують явного підтвердження.
